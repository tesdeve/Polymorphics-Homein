<%= form_with(model: conjunto, local: true) do |form| %>
  <% if conjunto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conjunto.errors.count, "error") %> prohibited this conjunto from being saved:</h2>

      <ul>
        <% conjunto.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
